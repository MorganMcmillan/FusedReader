# FusedReader: read multiple files as one

The file `FusedReader.lua` provides a class that allows the user to combine multiple files into one easy to use stream.

## Todo

- test that everything works
